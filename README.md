# API-rest-native

# 💄 Makeup App

App React Native (Expo + TypeScript) que consome a API pública de maquiagem:
https://makeup-api.herokuapp.com/api/v1/products.json

## 🚀 Como rodar

```bash
# Clone o repositório
git clone https://github.com/seuusuario/makeup-app.git
cd makeup-app

# Instale as dependências
npm install

# Inicie o app
npx expo start
